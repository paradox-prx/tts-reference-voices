# Experiment log

Every experiment run on this machine, in order: why it ran, the exact configuration, where its outputs are and the
headline numbers. Full per-request numbers and every generated take (AI-generated audio; never publish) are under
`server/results/<experiment>/` (gitignored, local only). `server/results/INDEX.md` indexes the benchmark phases
(generated by `server/bench/collect.py`).

Machine: `vector`, 1x RTX 3090 24 GB (GPU 0, also drives the desktop: ~0.35 GB and some GPU time), driver 580.95.05
(CUDA 13.0), i9-14900K (governor powersave, EPP balance_performance), 30 GB RAM, Ubuntu 25.04. Engine: vllm 0.28.0 +
vllm-omni 0.28.0 + torch 2.13.0 cu130, `VLLM_USE_FLASHINFER_SAMPLER=0`. Model Qwen/Qwen3-TTS-12Hz-1.7B-Base
(snapshot fd4b2543), bf16 talker, fp32 Code2Wav (0.28 default). Times are Asia/Karachi.

## 2026-09-25

### E00a: first engine start (FAILED): `logs/engine_smoke_default.log`
Upstream default deploy YAML, default FlashInfer sampler. Stage-0 init JIT-compiles FlashInfer's sampling kernels and
fails: `cuda_toolkit.h:41: #error "CUDA compiler and CUDA toolkit headers are incompatible"`. pip resolved
nvidia-cuda-nvcc 13.4.92 while the CUDA runtime headers are 13.0 (CUDART_VERSION 13000), and FlashInfer's bundled
CCCL 3.3.2 refuses a compiler/header mismatch. Fix used from here on: `VLLM_USE_FLASHINFER_SAMPLER=0` (vLLM's PyTorch
top-k/top-p sampler; the talker's codec vocabulary is only 3072 entries). No JIT is needed at all then.

### E00b: engine start with the prod YAML: `logs/engine_smoke_prod.log`
`engine/deploy/qwen3_tts_prod.yaml` (stage 0 gpu_memory_utilization 0.45, stage 1 0.18, max_num_seqs 64,
async_chunk true, CUDA graphs on), GPU 0.

| item | value |
|---|---|
| free VRAM at start | 22.96 / 23.56 GiB |
| stage 0 weights + non-torch | 4.15 GiB |
| stage 0 activation peak (max_num_batched_tokens 32768) | 1.5 GiB |
| stage 0 CUDA graphs | 0.17 GiB |
| stage 0 KV cache | 6.2 GiB = **58,032 tokens** ("max concurrency at 4,096 tokens/request: 14.17x") |
| stage 1 (Code2Wav) | model 0.48 GiB + graphs 0.12 GiB; ~4.2 GiB reserved in total |
| whole engine on the card (nvidia-smi, incl. ~0.35 GB desktop) | 15.2 GB idle, 16.0-17.0 GB under load |
| startup | stage 0 init 74 s (torch.compile 17 s), stage 1 2 s, ~95 s to "startup complete" |

### E00c: first syntheses, engine direct, c=1: `results/00_first_engine_smoke/`
Both voices × inline `ref_audio` / registered voice × non-stream / stream, one short sentence each.

| voice | mode | stream | total s | TTFA s | audio s | RTF |
|---|---|---|---|---|---|---|
| trump | inline | no | 1.589 (cold ref encode) | | 4.40 | 0.361 |
| trump | inline | yes | 0.798 | 0.115 | 4.08 | 0.195 |
| trump | registered | no | 1.009 | | 5.20 | 0.194 |
| trump | registered | yes | 0.784 | 0.111 | 4.08 | 0.192 |
| shehbaz | inline | no | 1.140 | | 5.44 | 0.210 |
| shehbaz | inline | yes | 1.161 | 0.117 | 6.16 | 0.188 |
| shehbaz | registered | no | 1.216 | | 6.40 | 0.190 |
| shehbaz | registered | yes | 1.099 | 0.114 | 5.92 | 0.186 |

Whisper large-v3 (CPU int8, forced language; `asr_cpu_int8.json`): English verbatim in all four takes; Urdu
intelligible with small accent errors (e.g. مجد for معیشت, ملک اور مہند for مل کر محنت).

### E00d: first requests through the gateway: `results/00_first_gateway_smoke/`
Gateway (registered voices, warmup, retries 1, length cap, pace band) in front of E00b. Bring-up: both voices
uploaded + warmed up (0.59 / 0.79 s) → ready in 1.4 s.

| run | wall s | audio s | aggregate x realtime | notes |
|---|---|---|---|---|
| c=1, 4 requests (2 voices × stream/non-stream) | | | | TTFA 0.110-0.119 s streaming |
| c=8 non-stream (4 trump + 4 shehbaz short) | 3.10 | 45.8 | 14.8 | pace ratio trump 0.78-0.88, shehbaz 1.02-1.50; 0 suspect |
| c=8 stream | 2.51 | 41.3 | 16.4 | TTFA 0.15-0.63 s |

### E01: scaling probe, engine direct: `results/01_probe_scaling_engine_direct/`
Exploratory (before the full plan): committed `bench_tts.py` (copy in the folder), inline references, `long` pool
texts (55-70 words; ~16 s trump / ~22 s shehbaz), non-stream, sweep c = 1, 4, 8, 16, 32, 48, 64, n = max(8, 2c).

| voice | c | n | lat p50 s | p90 | p99 | x realtime | req/s | GPU MiB |
|---|---|---|---|---|---|---|---|---|
| trump | 1 | 8 | 2.71 | 3.06 | 3.36 | 5.59 | 0.36 | 15925 |
| trump | 4 | 8 | 3.78 | 4.17 | 4.64 | 15.58 | 0.98 | 15923 |
| trump | 8 | 16 | 6.24 | 7.00 | 7.74 | 19.46 | 1.20 | 15923 |
| trump | 16 | 32 | 9.00 | 10.14 | 12.79 | 26.29 | 1.59 | 15975 |
| trump | 32 | 64 | 13.56 | 15.94 | 17.15 | 35.06 | 2.17 | 16203 |
| trump | 48 | 96 | 21.27 | 24.77 | 27.04 | 34.24 | 2.13 | 16475 |
| trump | 64 | 128 | 24.15 | 30.26 | 35.88 | 38.49 | 2.37 | 16752 |
| shehbaz | 1 | 8 | 4.05 | 4.29 | 4.58 | 5.55 | 0.25 | 16749 |
| shehbaz | 4 | 8 | 5.28 | 5.55 | 5.60 | 15.71 | 0.74 | 16750 |
| shehbaz | 8 | 16 | 8.55 | 10.03 | 10.25 | 19.15 | 0.87 | 16748 |
| shehbaz | 16 | 32 | 11.67 | 13.71 | 14.24 | 27.58 | 1.27 | 16749 |
| shehbaz | 32 | 64 | 18.72 | 21.72 | 22.71 | 35.52 | 1.58 | 16753 |
| shehbaz | 48 | 96 | 28.61 | 33.71 | 39.47 | **21.98** | 1.00 | 16753 |
| shehbaz | 64 | 128 | 32.89 | 40.29 | **113.56** | **22.70** | 1.02 | 16966 |

GPU utilisation 96-100% from c=1. No preemption was logged. The shehbaz collapse at c ≥ 48 comes from **Urdu
runaways**: the engine log shows 5 "failed generation validation; retrying once" events, all during shehbaz c48/c64
(~1.4% of 352 Urdu requests; 0 of 352 English). Without `max_new_tokens` the engine's own cap is 12 frames per text
token (~2,300 frames ≈ 186 s for a 60-word Urdu text), so each runaway occupies a batch slot for minutes before its
single built-in retry, which is what produced the 96-125 s tails. For comparison, published 3090 numbers for this
model (vLLM-Omni 0.25, PR #5253) were 4.07x at c=1 and 17.39x at c=64.

### E02: the same Urdu cell through the gateway: `results/02_probe_gateway_urdu_c48/`
shehbaz, long, c=48, n=96, through the gateway (max_inflight 32 → 16 queue, retries 1, pace-based length cap:
`min(words formula, 1.2 × 1.8 × expected duration + 2 s)` ≈ 40 s for these texts).

| path | x realtime | lat p50 s | p90 | p99 | max | errors |
|---|---|---|---|---|---|---|
| engine direct (E01) | 21.98 | 28.61 | 33.71 | 39.47 | 95.8 | 0 |
| gateway | **34.34** | 23.83 | 35.70 | 40.81 | 57.5 | 0 |

One take hit the length cap (engine HTTP 500 codec limit) and the gateway retried it: 57.5 s end to end, audio
22.8 s, pace ratio 1.10. The gateway's cap + retry + in-flight limit keep throughput at the c=32 level.

### E03: async_chunk off vs on: `results/03_ab_async_chunk/{no_async_chunk,async_chunk}/`
Single-variable A/B on the (by then updated) prod YAML: stage 0 gpu_memory_utilization **0.60** (KV 9.73 GiB =
**91,120 tokens**, "22.25x at 4,096 tokens"), stage 1 0.18. `--no-async-chunk` on the CLI vs the YAML default
(async_chunk true). Engine direct, inline references, `long` pool texts, committed `bench_tts.py`.

| voice | c | mode | lat p50 s | p99 | TTFA p50 s | x realtime | GPU MiB (incl. ~350 desktop) |
|---|---|---|---|---|---|---|---|
| trump | 1 | async off | 3.06 | 3.38 | | 5.26 | 22817 |
| trump | 1 | async on | 3.00 | 3.21 | | 5.55 | 19156 |
| trump | 8 | async off | 7.28 | 8.02 | | 16.51 | 22817 |
| trump | 8 | async on | 6.50 | 7.16 | | 19.01 | 19186 |
| trump | 32 | async off | 15.92 | 20.47 | | 28.56 | 22965 |
| trump | 32 | async on | 13.97 | 17.99 | | **34.48** | 19337 |
| trump stream | 1 | async off | 3.45 | 3.63 | 3.447 | 4.96 | 22967 |
| trump stream | 1 | async on | 2.89 | 3.53 | **0.115** | 5.54 | 19339 |
| trump stream | 8 | async off | 7.24 | 7.99 | 7.236 | 16.27 | 22968 |
| trump stream | 8 | async on | 6.12 | 7.73 | **0.333** | 18.95 | 19339 |
| shehbaz | 1 | async off | 4.49 | 4.92 | | 4.90 | 23187 |
| shehbaz | 1 | async on | 4.05 | 4.88 | | 5.49 | 19642 |
| shehbaz | 8 | async off | 9.69 | 11.35 | | 17.14 | 23186 |
| shehbaz | 8 | async on | 8.70 | 10.37 | | 18.30 | 19643 |
| shehbaz | 32 | async off | 21.81 | 25.90 | | 18.38* | 23193 |
| shehbaz | 32 | async on | 18.76 | 24.19 | | 21.60* | 19647 |
| shehbaz stream | 1 | async off | 4.53 | 5.57 | 4.528 | 4.91 | 23186 |
| shehbaz stream | 1 | async on | 4.22 | 4.76 | **0.136** | 5.43 | 19639 |
| shehbaz stream | 8 | async off | 9.56 | 11.04 | 9.559 | 16.28 | 23188 |
| shehbaz stream | 8 | async on | 9.13 | 9.80 | **0.416** | 19.07 | 19643 |

\* each shehbaz c=32 run had exactly one Urdu runaway (engine log: 1 "failed generation validation; retrying once"
per engine); that single straggler (65 s / 76 s end to end) set the run's wall time, halving aggregate throughput
(E01 without a runaway: 35.5x).

**Conclusion:** on vllm-omni 0.28 on this 3090, async_chunk **on** is better on every axis: +5-21% throughput, TTFA
0.1-0.4 s instead of the whole generation, and 3.5-4.5 GB less VRAM (async off pushed the card to 23.2 of 24 GB,
which leaves the desktop almost nothing). The older reports that async off is 15-70% faster (#4371, vLLM-Omni
0.20-0.22) predate 0.27's cached incremental decode. Keep async_chunk on; drop no_async_chunk from further screening.

### E04: do the knobs we benchmark actually change behaviour? `results/04_knob_verification/`
Engine: `engine/deploy/variants/custom_voices.yaml` (prod YAML + `custom_voice_dir: state/custom_voices`), with the
per-request repetition-penalty patch applied. Startup log: "Loaded 4 precomputed Qwen3-TTS custom voice profile(s)"
(`trump-avg`, `trump-prompt`, `shehbaz-avg`, `shehbaz-prompt`; `engine/precompute_voices.py`: ICL reference codes of
`references/qwen3-tts.wav`; `-avg` = mean of the per-clip x-vectors rescaled to the mean clip norm, `-prompt` = the
reference clip's own x-vector; cosine(avg, prompt) 0.995 shehbaz, 0.997 trump).

**Seeds do not reproduce on 0.28 (CUDA graphs on), even one request at a time.** The same request with `seed: 1234`
twice gave different audio (trump 67 vs 62 codec frames; shehbaz 109 vs 93). So hash-equality checks are useless,
takes cannot be paired by seed across settings, and every experiment uses unseeded independent takes (seeds would
also serialize the code predictor inside a batch and disable the engine's retry). Paired analyses pair by prompt.

Knob checks (a: 18 single requests, `results.json`; b: validation + 8 takes per setting, `b_validation_and_effects/`):

| field | evidence it is read | verdict |
|---|---|---|
| `extra_params.repetition_penalty` | -1 → 400 "must be a finite number > 0" (our patch) | read (patched engine) |
| top-level `repetition_penalty` | -1 → 200 | silently ignored |
| `extra_params.temperature` | -1 → 400 "temperature must be non-negative" | read |
| top-level `temperature` | -1 → 200 | silently ignored |
| `extra_params.top_k` | 0 → 200 (vLLM: 0 = no top-k) | read (0 is legal) |
| `language` | "Urdu" → 400 "Invalid language" | read |
| `non_streaming_mode` | "x" → 400; source: serving_speech.py:2577-2579 → prompt_embeds_builder.py:952-963, 896 (Base default False) | read |
| `voice` = custom voice | unknown name → 400; `trump-avg` / `shehbaz-avg` → 200 | read |
| `max_new_tokens` | 40 → HTTP 500 "did not emit codec EOS before its token budget (40/40)" | read |

Mean audio duration over 8 takes (trump, one 18-word sentence): default 5.19 s (sd 0.21), rp 3.0 4.89 (0.19),
temperature 0.05 5.25 (0.30), temperature 1.5 4.98 (0.37), non_streaming_mode true 4.99 (0.47), language Auto 5.13
(0.17). Duration barely moves, so the effect of these knobs is measured with ASR / speaker similarity (quality eval),
not with duration.

### E05: eval / QC stack on the GPU: `results/05_eval_gpu_check/gpu_check_cuda.json`
`eval/gpu_check.py` (one process: faster-whisper large-v3 fp16 via CTranslate2 4.8.2 + cuBLAS 12.9.2, and WavLM
base-plus-sv + WavLM-Large/ECAPA via torch 2.13 cu130 + cuBLAS 13.1.1), engine stopped. Both cuBLAS libraries load
side by side. Models load in 4.2 s; the process holds **6.2 GB** of VRAM (ASR with 2 workers + both SIM models).
One 10.2 s Urdu reference clip: cold 0.76 s (ASR 685 ms, SIM 63 ms), **warm 0.54 s (ASR 474 ms, SIM 56 ms, audio
checks 8 ms) = 19x realtime**, 2 clips in parallel 25x; WER 0.056, CER-nospace 0.015, SIM-prompt base 0.984 /
large 0.901. On CPU the same call takes ~5.6 s (int8), too slow for online QC. Consequence: online QC must run on
the GPU, and 6 GB does not fit next to the engine at stage-0 0.60 (~4.4 GB free) → with QC the engine runs stage 0
at 0.45 (KV 58k tokens, still preemption-free at c<=32 for 60 s takes).

### P1 notes (benchmark phases are indexed in `results/INDEX.md`)
- `fp16_talker` and `fp32_talker` crash on the first request (`index_copy_(): ... Half/Float and BFloat16`): the
  speaker encoder / codec encoder are hard-coded bf16 in vllm-omni 0.28 (`logs/engine_P1_screen_fp{16,32}_talker.log`).
  bf16 is the only working talker precision on 0.28; Code2Wav is fixed at fp32 in 0.28.
- `decode8` (Code2Wav batch 8 + graph buckets 1/2/4/8) OOMs at start at stage-0 0.60 (stage 1 capturing its graphs
  finds 43 MB free); re-screened as `decode8_m045` next to `default_m045` (both stage-0 0.45).
